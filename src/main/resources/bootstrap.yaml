spring:
  application:
    name: netflix-zuul-api-gateway-server
  cloud:
    config:
      uri: http://localhost:8888

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka

server:
  port: 8765
